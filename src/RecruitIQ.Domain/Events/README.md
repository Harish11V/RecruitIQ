# Domain Events

This folder will contain Domain Events representing significant occurrences in the business domain.
Examples: `JobPostedEvent`, `CandidateAppliedEvent`, `InterviewScheduledEvent`.
These can be dispatched to notify handlers across the application.
